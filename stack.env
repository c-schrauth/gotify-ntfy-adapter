# --------------------------------------------------
# Gotify -> ntfy Adapter
# Stack Environment File (Portainer)
# --------------------------------------------------

# === ntfy Zielserver ===
NTFY_SERVER=https://ntfy.deinedomain.tld

# ntfy Publisher-User (Passwort kommt aus Secret!)
NTFY_USERNAME=pub-adapter

# Pfad zum Docker Secret (Portainer)
NTFY_PASSWORD_FILE=/run/secrets/ntfy_password


# === Topic-Konfiguration ===

# Fallback-Topic, falls Token nicht gemappt ist
DEFAULT_TOPIC=monitoring-adapter-info

# Token -> Topic Mapping aus Secret
TOKEN_TOPIC_MAP_FILE=/run/secrets/token_topic_map


# === Payload-Defaults ===

# Tags, die jede Nachricht bekommt
DEFAULT_TAGS=gotify,adapter

# Optionaler Prefix für Titel
TITLE_PREFIX=[Gotify] 


# === Optional: Debug / Erweiterungen ===
# (aktuell im Code nicht genutzt, aber vorbereitet)
# LOG_LEVEL=info
